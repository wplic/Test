<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
		"http://www.ibatis.com/dtd/sql-map-2.dtd">

<!-- iBatis SQL Map 文件 -->
<sqlMap namespace="com.cssweb.theme.pojo.ThemePic">

	<typeAlias alias="ThemePic" type="com.cssweb.theme.pojo.ThemePic" />

	<resultMap id="ThemePicMap" class="ThemePic">
		<result property="id" column="ID" jdbcType="Long"/>
		<result property="categoryid" column="CATEGORYID" jdbcType="Long"/>
		<result property="title" column="TITLE" jdbcType="String"/>
		<result property="content" column="CONTENT" jdbcType="String"/>
		<result property="updatetime" column="UPDATETIME" jdbcType="Date"/>
		<result property="picid" column="PICID" jdbcType="Long" nullValue="0"/>
	</resultMap>

	<resultMap id="ThemePicMap2" class="ThemePic">
		<result property="id" column="ID" jdbcType="Long"/>
		<result property="category" column="CATEGORY" jdbcType="String"/>
		<result property="title" column="TITLE" jdbcType="String"/>
		<result property="content" column="CONTENT" jdbcType="String"/>
		<result property="updatetime" column="UPDATETIME" jdbcType="Date"/>
		<result property="picid" column="PICID" jdbcType="Long" nullValue="0"/>
	</resultMap>

	<insert id="insert" parameterClass="ThemePic">
		INSERT into T_THEME_PIC (ID,CATEGORYID,TITLE,CONTENT,UPDATETIME) VALUES (#id#,#categoryid#,#title#,#content#,sysdate)
	</insert>
	
	<update id="update" parameterClass="ThemePic">
		update T_THEME_PIC set CATEGORYID=#categoryid#,TITLE=#title#,CONTENT=#content#,UPDATETIME=sysdate where ID=#id#
	</update>
	
	<update id="updatePicid" parameterClass="java.util.Map">
		update T_THEME_PIC set PICID=#picid# where ID=#id#
	</update>

	<delete id="delete" parameterClass="String">
		delete from T_THEME_PIC where ID in ($value$)
	</delete>
	
	<delete id="deleteById" parameterClass="Long">
		delete from T_THEME_PIC where ID = #value#
	</delete>
		
	<select id="getAll" resultMap="ThemePicMap">
		select * from T_THEME_PIC ORDER BY ID
	</select>

	<select id="getCountByCategory" parameterClass="Long" resultClass="java.lang.Integer">
		select count(*) from T_THEME_PIC  where CATEGORYID = #categoryid#
	</select>

	<select id="getCountBySql" parameterClass="String" resultClass="java.lang.Integer">
		$sql$
	</select>

	<select id="getById" parameterClass="Long" resultMap="ThemePicMap">
		select * from T_THEME_PIC where ID= #id#
	</select>	
	
	  <select id="getFrontByMap" parameterClass="java.util.Map" resultMap = "ThemePicMap">
    	SELECT * FROM ( 
    		SELECT row_.*, ROWNUM rownum_ FROM (
    		 	$sql$
    		 )
			row_ WHERE ROWNUM &lt;= #end# 
		) WHERE rownum_ &gt;#begin#
  	</select>
	
	  <select id="getAdminByMap" parameterClass="java.util.Map" resultMap = "ThemePicMap2">
    	SELECT * FROM ( 
    		SELECT row_.*, ROWNUM rownum_ FROM (
    		 	$sql$
    		 )
			row_ WHERE ROWNUM &lt;= #end# 
		) WHERE rownum_ &gt;#begin#
  	</select>
	
</sqlMap>
